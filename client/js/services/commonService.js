angular.module('app').factory('commonService', ['$q', '$http', function($q,$http,) {
  return({

  });
  function common(){

  };


  // function alertMessage(message, type, $alert) {
           // var alertMessage = 'Sorry something went wrong, Please try again later';
           // var hint = '<br> Hint: ';
           // animation = 'am-fade-and-scale';
           // return function() {
           //     $alert({
           //         title: type,
           //         content: (type == 'error') ? ((message) ? (alertMessage + hint + message) : alertMessage) : message,
           //         placement: 'top-right',
           //         type: type,
           //         animation: animation,
           //         keyboard: true,
           //         duration: 5
           //     });
           // }();
       // }



}]);
